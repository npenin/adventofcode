import { Day } from "../../aoc.contracts";

export const day: Day<any> = {
    parse(input)
    {

    },
    compute(values)
    {
        return 0;
    },

    sampleExpectedValue: 0
}

export default day;